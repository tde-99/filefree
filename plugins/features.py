#Centralized features in plugins/settings.py
from bot import Bot
from pyrogram import filters
from helper_func import is_admin
from database.database import db

# Features file is now mostly empty as logic is moved to centralized settings
